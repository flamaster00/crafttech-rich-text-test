export type TShape = {
    x: number,
    y: number,
    width: number,
    height: number,
    html: string,
    id: string,
    text: string,
    type: string
}

export type TTool = 'shape' | 'cursor'